torch>=2.1.0
torchvision
torchaudio
# Pinned ML stack — known-good, mutually compatible (Qwen2.5 + LoRA + DPO).
# transformers<4.46 keeps `evaluation_strategy`; trl 0.11 keeps the classic
# SFTTrainer/DPOTrainer signatures. src/train.py has a version-agnostic
# eval-strategy helper so it also runs on newer stacks.
transformers==4.44.2
trl==0.11.4
peft==0.13.2
accelerate==0.34.2
datasets==3.0.1
bitsandbytes==0.44.1
# NOTE: if torchao is installed, PEFT's LoRA dispatcher raises ImportError when
# the version is below 0.16.0. We don't use torchao — run `pip uninstall -y torchao`
# or rely on the _disable_torchao_dispatch() shim in src/train.py.
sentencepiece
protobuf
huggingface-hub
wandb
tensorboard
pyyaml
jinja2
requests
scipy
numpy
pandas
autoawq
onnx
onnxruntime
onnxruntime-directml
jupyter
evalplus>=0.2.0